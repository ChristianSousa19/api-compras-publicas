# Configurações do Banco de Dados
# Para este projeto, o padrão SQLite é suficiente.
DATABASE_URL="sqlite:///./compras_publicas.db"

# Configurações de Segurança JWT
# IMPORTANTE: Gere uma nova chave secreta para o seu arquivo.env real.
# Você pode usar o comando: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=seu_segredo_super_secreto_de_32_caracteres_aqui
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7